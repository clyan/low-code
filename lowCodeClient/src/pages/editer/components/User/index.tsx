import { Button } from 'antd';
import './index.less'
export default function User() {
    return (
        <div className = "user"> 
            {/* <Button ghost > 登录 </Button> */}
        </div>
    );
}